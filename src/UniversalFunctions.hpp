#include <iostream>
#include <vector>
#pragma once
//#include "PlanetaryBody.hpp"
/*
set of functions related to gravitational interaction between entities
*/
//namespace Gravity_Components {

//}

/*
functions that are used for building components and the environment 
*/
//namespace Build_Functions {
	
	//void buildEntity() {

	//}
	/*
	Function that creates a planet object,
	inputs: name, origin, mass, radius, atmosphere model
	output : PlanetaryBodies object
	*/
	//PlanetaryBody buildPlanetaryBodies(std::string name, const Vec3& origin, Vec3& pos, float mass, float radius, std::vector<std::string> CSVPATHS, OrbitParams orbit_params, RotationFrame rotation_frame) {
		//PlanetaryBody p = PlanetaryBody(name, origin, pos, mass, radius, CSVPATHS, orbit_params, rotation_frame);
		//return p;
	//}
//}